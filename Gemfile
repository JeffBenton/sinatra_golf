source "https://rubygems.org"

gem 'sinatra'
gem 'sqlite3'
gem 'activerecord', :require => 'active_record' # activerecord is the gem, active_record is the library
gem 'rake'
gem 'sinatra-activerecord', :require => 'sinatra/activerecord'
gem 'require_all'
gem 'bcrypt'
gem 'shotgun'

group :development do
  gem 'pry'
  gem 'tux'
end